import {
  red, gold, black, yellow,
} from '@q/colors';

export default {
  lavalamp: `radial-gradient(${red} 0%, ${black} 60%, ${gold} 70%);`,
  desk: `radial-gradient(${black} 20%, ${yellow} 80%);`,
};
