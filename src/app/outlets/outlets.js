import {
  red, gold, black, yellow,
} from '@q/colors';

export default {
  lavalamp: `radial-gradient(circle, ${red} 35%, ${black} 37%, ${gold} 100%);`,
  desk: `radial-gradient(circle, ${black} 35%, ${yellow} 40%, ${black} 95%);`,
};
